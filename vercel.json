{
  "buildCommand": "pnpm build && pnpm db:migrate:deploy",
  "outputDirectory": "dist/spa",
  "installCommand": "pnpm install",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
